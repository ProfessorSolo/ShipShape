---
title: "Anatomy of a Dockerfile"
description: "The baseline recipe used to build an image."
sidebar:
  order: 7
  label: "Anatomy of a Dockerfile"
  draft: false
---

import { LinkButton } from "@astrojs/starlight/components";
import NoteBox from "../../../components/NoteBox.astro";
import CautionBox from "../../../components/CautionBox.astro";

## The Build Recipe

A **`Dockerfile`** is the literal text file recipe used to build an image.

It tells the Docker Engine exactly what to start with, what to do, and which command to run when an instance finally starts up.

Here is a super-simple, example `Dockerfile` for a basic Node application:

```dockerfile title="Dockerfile"
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3000

CMD ["npm", "start"]
```

<NoteBox title="Hang Tight">
  We'll break down each instruction in this Dockerfile in the next section.
  After that, we'll write our own to containerize an app.
</NoteBox>

This is small, but it does a massive amount of heavy lifting.

It translates directly to:

- start from a lightweight Node 20 base image
- set up shop inside an `/app` directory
- copy only the package manifest files first
- install the `node_modules` dependencies
- copy the rest of the application files over
- document that the app listens on port 3000
- define the default start command

With just those lines, we've defined exactly how our application should be packaged.

<CautionBox title="Capitalization Matters">
  By strict convention, a `Dockerfile` must be named exactly that. This means a
  capital `D` and **no file extension**.

Docker Engine will look for this Dockerfile, automatically, in the project
root.

</CautionBox>

---

## Extra Bits & Bytes

<LinkButton
  href="https://docs.docker.com/reference/dockerfile/"
  icon="external"
  variant="secondary"
  target="_blank"
>
  Dockerfile Reference Docs
</LinkButton>
---
<LinkButton
  href="https://hub.docker.com/"
  icon="external"
  variant="secondary"
  target="_blank"
>
  Browse Docker Hub
</LinkButton>

### ⏭ The Six-Pack

There are six instructions that are almost always present in a Dockerfile. Next, we'll break them down, one by one.
